extends layout
block content
 h1 Energy Scraper
 - var url = 'metrics';
 h3 Values only exported via #[a(href='/' + url) /metrics] endpoint.
 hr
 div#version Energy Scraper Version: #{version}
   br
   | Running on #{hostname}
   br
   | Azure build #: #{azureBuildNumber}